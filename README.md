# CONTACT LIST API

### Create Virtualenv 

    $ pipenv shell

### Install Libraries

    $ pipenv install

### Init migrations

    $ pipenv run init

### Create migrations

    $ pipenv run migrate

### Create tables on database

    $ pipenv run upgrade

### Start Server

    $ pipenv run start

